rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // BLOQUEIO TOTAL: nenhum cliente acessa direto.
    // Somente Cloud Functions (Admin SDK) tem acesso.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
